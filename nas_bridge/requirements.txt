discord.py>=2.5,<3
fastapi>=0.116,<1
pydantic>=2.11,<3
pydantic-settings>=2.10,<3
SQLAlchemy>=2.0,<3
uvicorn[standard]>=0.35,<1
# F11/SDK: BridgeV2Client uses httpx. agent_sdk module hard-imports
# httpx at the top. Bundled so external clients work out-of-the-box.
httpx>=0.27,<1
# Note: anthropic SDK is intentionally NOT bundled in the default
# image. Production AI path is BRIDGE_AGENT_BRAIN=pc-claude which
# dispatches to a PC running claude_executor (uses the user's local
# Claude session). The api-key brain (BRIDGE_AGENT_BRAIN=claude) is
# kept in code for unit tests + non-PC deployments; install anthropic
# manually if you need it.
